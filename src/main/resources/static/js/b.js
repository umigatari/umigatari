document.addEventListener("DOMContentLoaded", () => {
    const message = document.querySelector(".message");
    if (message) {
        message.style.animation = "fadeIn 0.8s ease-out forwards";
    }
});
